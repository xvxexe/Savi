# Livornolife site demo

## Avvio locale

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

Il progetto è già configurato con un workflow in `.github/workflows/deploy.yml`.

Se il nome della repository non è `Savi`, aggiorna `base` in `vite.config.js`.
